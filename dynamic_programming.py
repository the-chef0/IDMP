import numpy as np

def get_true_gradients(weights: np.array, features: np.array, \
    values: np.array, alpha_values: np.array):
    # TODO: Return an array containing the true SPO gradient value
    # for each value in alpha_values
    return np.random.rand(len(alpha_values))
